// obtener promedio de numeros
 const numbers = [5, 3, 4, 7, 2, 1, 9, 7, 7]


function calcularMediAritmetica(numbers){

const sumaNumbers = list.reduce(
    function(valorAcumulado = 45, nuevoElemento) {
        return valorAcumulado + nuevoElemento;
    }
);

const promedioNumbers = sumaNumbers / numbers.length;

return promedioNumbers

}

console.log(promedioNumbers)